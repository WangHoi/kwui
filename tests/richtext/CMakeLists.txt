kwui_add_executable(test_richtext)
target_sources(test_richtext PRIVATE richtext.cpp)
if(WIN32)
  target_sources(test_richtext PRIVATE dpi-aware.manifest)
endif()
target_include_directories(test_richtext PRIVATE ${CMAKE_SOURCE_DIR}/src/api)
target_link_libraries(test_richtext kwui_static)
