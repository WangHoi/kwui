kwui_add_executable(test_custom_font)
target_sources(test_custom_font PRIVATE custom_font.cpp)
if(WIN32)
  target_sources(test_custom_font PRIVATE dpi-aware.manifest)
endif()
target_include_directories(test_custom_font PRIVATE ${CMAKE_SOURCE_DIR}/src/api)
