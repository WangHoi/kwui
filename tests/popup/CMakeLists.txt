kwui_add_executable(test_popup)
target_sources(test_popup PRIVATE popup.cpp)
if(WIN32)
    target_sources(test_popup PRIVATE dpi-aware.manifest)
endif()
target_include_directories(test_popup PRIVATE ${CMAKE_SOURCE_DIR}/src/api)
