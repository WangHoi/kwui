kwui_add_executable(test_native)
target_sources(test_native PRIVATE native_main.cpp)
if (WIN32)
    target_sources(test_native PRIVATE dpi-aware.manifest)
endif ()
target_include_directories(test_native PRIVATE ${CMAKE_SOURCE_DIR}/src/api)
